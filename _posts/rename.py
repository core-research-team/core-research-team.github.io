import os
from os import rename


files=os.listdir('.')
for f in files:
	if sys.argv[0].split("\\")[-1] == name:
        continue
	else
		print(f)
		rename(f,f.replace('Untitledd','Untitled'))
		#rename(f,f.replace(f[0:10],'Untitled'))

print("===============")

files=os.listdir('.')

for f in files:
	print(f)